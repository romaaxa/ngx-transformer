import { NumbroFormatNumberPipe } from './numbro-format-number.pipe';

describe('NumbroFormatNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new NumbroFormatNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
