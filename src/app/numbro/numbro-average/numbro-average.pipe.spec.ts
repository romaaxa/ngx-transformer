import { NumbroAveragePipe } from './numbro-average.pipe';

describe('NumbroAveragePipe', () => {
  it('create an instance', () => {
    const pipe = new NumbroAveragePipe();
    expect(pipe).toBeTruthy();
  });
});
